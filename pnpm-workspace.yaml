packages:
  - packages/*

# Versions live here, not in each package, so three packages cannot drift onto
# three different TypeScripts. Referenced as `catalog:` in package.json.
catalog:
  typescript: ^6.0.3
  vitest: ^3.2.7
  '@vitest/coverage-v8': ^3.2.7
  '@types/node': ^24.0.0
  tsx: ^4.19.2
  '@stryker-mutator/core': ^10.0.0
  '@stryker-mutator/vitest-runner': ^10.0.0
  jscpd: ^5.0.15
