## 1. The gate tells the truth about faults

- [x] 1.1 Red → green: the parity gate splits rejection rows into those a reader can see and those
      only a search can find, naming the excused kind and its reason.
- [x] 1.2 Red → green: a kind not named is still required to refuse, so a second exception is a
      visible edit.

## 2. Read a Kakuro

- [x] 2.1 Red → green: the model and reader — runs with sums, a domain of nine, structural checks.
- [x] 2.2 Red → green: the parity gate reports 4 readable, 1 pending.
- [x] 2.3 Red → green: a run sum outside what its length could reach is refused.

## 3. Clues on the board

- [x] 3.1 Red → green: a run's sum is drawn on its first cell with a direction.
- [x] 3.2 Red → green: a cell starting two runs shows both.
- [x] 3.3 Register it and keep the overflow gate green.

## 4. Content and evidence

- [x] 4.1 Author a Kakuro into the pack and emit it.
- [x] 4.2 Tier 1 with counts, Tier 1b matrix, Tier 2 on the simulator.
