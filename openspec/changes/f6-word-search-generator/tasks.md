## 1. Placement

- [x] 1.1 Red → green: every listed word appears exactly once, in a rectangular grid of the
      right size, with every cell a letter the contract admits.
- [x] 1.2 Red → green: the whole grid is used and more than two directions appear.
- [x] 1.3 Red → green: a word as long as the grid is wide is placed, not dropped.

## 2. The batch

- [x] 2.1 Red → green: the loop is shared with the caged generator and a proposer names its own
      refusal.
- [x] 2.2 Red → green: a vocabulary nothing fits exhausts the budget and says which reason.

## 3. The adapter

- [x] 3.1 `--kind wordSearch`, with the vocabulary and the copy as content beside it.

## 4. Evidence

- [x] 4.1 Tier 1 with counts, Tier 1b (`npm run mutation`, `npm run dry`).
