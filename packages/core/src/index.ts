/**
 * `@akimath/core` — the rederivation machine.
 *
 * Re-export only; no logic lives here, which is the precedent
 * `packages/contract`'s own index sets. Zero runtime dependencies, and a test
 * reads the manifest to keep it that way.
 */
export {};
